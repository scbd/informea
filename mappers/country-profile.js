var _            = require('underscore');var superagent   = require('superagent');var superagentQ  = require('superagent-promises');var when         = require('when');var guard        = require('when/guard');                   require('when/es6-shim/Promise');//============================================================// ctor////============================================================function CountryProfileMapper () {    } //============================================================//////============================================================CountryProfileMapper.prototype.query = function (aaa, bbb) {    	var response = superagent.get('https://chm.cbd.int/api/v2013/countries').use(superagentQ).end();	return when(response, function(res) {		var results = [];					res.body.forEach(function (countryProfile) {			console.log(countryProfile);			results.push(new CountryProfile({                protocolVersion:        2,                country:                countryProfile.code,                treaty:                 'cbd',                entryIntoForce:         countryProfile.treaties.XXVII8.party,            //  updated:                                entryIntoForceStatus:   countryProfile.treaties.XXVII8.instrument            //  statusDate:                         //  note:			}));		});		return results;	});}module.exports = exports = new CountryProfileMapper();